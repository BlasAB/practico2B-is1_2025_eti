<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear una cuenta</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Crear una cuenta</h1>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        <form action="/user/new" method="post" class="space-y-4">
            <div>
                <label for="name" class="block text-gray-700 text-sm font-medium mb-1">Nombre de Usuario:</label>
                <input type="text" id="name" name="name" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <div>
                <label for="password" class="block text-gray-700 text-sm font-medium mb-1">Contraseña:</label>
                <input type="password" id="password" name="password" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <button type="submit"
                    class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition duration-200">
                Registrar Usuario
            </button>
            
            <!-- Bloque de enlaces debajo del formulario -->
            <div class="text-center mt-4 space-y-2">
                <!-- Enlace que devuelve al usuario a la página principal ("/") -->
                <a href="/" class="text-blue-600 hover:underline text-sm block">Volver al inicio</a>
                
                <!-- Enlace adicional pensado para profesores: 
                     redirige al formulario de registro de profesor en la ruta "/profesor/registrar".
                     Se muestra en verde para diferenciarlo del enlace de usuario -->
                <a href="/profesor/registrar" class="text-green-600 hover:underline text-sm block">¿Sos profesor? Registrate acá</a> 
            </div>
        </form>
    </div>
</body>
</html>